apiVersion: batch/v1
kind: Job
metadata:
  name: django-static-job
spec:
  template:
    spec:
      containers:
        - name: django-static
          image: victorysokolov/django-blog:e75ad3d65732d4a9d8980674f4a93dc9c5e14134
          imagePullPolicy: Always
          command: ['/bin/sh', '-c']
          args:
            - python manage.py collectstatic --noinput && python manage.py compress --force
          env:
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: /secrets/key.json
          envFrom:
            - secretRef:
                name: djapp
          volumeMounts:
            - name: google-credentials-volume
              mountPath: /secrets
              readOnly: true
            - name: static-storage
              mountPath: /app/output/static/
      volumes:
        - name: google-credentials-volume
          secret:
            secretName: google-credentials
        - name: static-storage
          persistentVolumeClaim:
            claimName: static-storage-pvc
      restartPolicy: Never
  backoffLimit: 1
